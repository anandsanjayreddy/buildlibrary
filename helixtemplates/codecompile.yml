steps:
- task: CmdLine@2
  inputs:
    script: |
      yarn install
      ls
      mkdir finalfolder
      cp -r ./node_modules ./finalfolder
      cd finalfolder
      zip -r web-0.1.0-snapshot-new-version.zip ./*
      ls
