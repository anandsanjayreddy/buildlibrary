steps:
- task: UniversalPackages@0
  displayName: Universal Publish
  inputs:
    command: publish
    publishDirectory: '$(System.DefaultWorkingDirectory)/finalfolder'
    vstsFeedPublish: 'HelixTesting/helix'
    vstsFeedPackagePublish: 'web-0.1.0-snapshot-new-version.zip'
    versionOption: custom
    versionPublish: '2.0.0'
    packagePublishDescription: 'Test package'

# - task: PublishPipelineArtifact@1
#   inputs:
#     targetPath: $(System.DefaultWorkingDirectory)/bin/WebApp
#     artifactName: WebApp